# Sulap Artisan Portal — Project Notes

Living reference for what's built, what each screen does, and the business rules baked into the code. Update this whenever a tab's behavior changes.

**Stack:** React + Vite, Supabase (installed, not wired), Netlify (deploy target, not set up)
**Status:** Phase 1 (UI, mock data) complete. Phase 2 (Supabase) and Phase 3 (Netlify) not started.
**Repo:** https://github.com/suriaenp/sulap-artisan

---

## Public site (`src/pages/PublicHome.jsx`)
- **Rebuilt 2026-07-12** from a Claude Design handoff (`Sulap Artisan Landing Page Design-handoff.zip`) into a full-bleed marketing page (sticky header, hero, "Coming Soon" carousel, "Why Join", CTA banner, footer). Renders standalone — `App.jsx`'s `AppShell` returns `<PublicHome/>` directly when `view==='public'`, bypassing the `.outer-wrap`/`.app-shell` phone-frame container, `Sidebar`, and `BottomNav` used by every other view.
- **"Coming Soon" carousel is static placeholder content** (4 hardcoded sample events with poster-gradient tiles, dates, names) — intentionally **not** wired to the live `events` store. If the app's event calendar should drive this section instead, that's a follow-up, not the current behavior.
- Header/CTA/footer buttons ("Apply as a Vendor", "Vendor Log In") still call the same `useStore()` navigation actions (`set({view:'vendor', vScreen:'register', ...})` etc.) as before — only the visuals changed. Footer adds a small "Admin" link (`goAdmin`) since the new header design has none.
- The old data-driven event-listing cards (pricing, open/closed-by-date badges) that used to live on this page were removed with the redesign; that logic (`open = !ev.lastApp || new Date(ev.lastApp) >= today`) still lives on and gates applying in the Vendor Dashboard's "Available Markets" tab.
- **Updated 2026-07-13** from a second Claude Design export with several changes: removed the hero "now accepting applications" badge; added a new dark "Our Gallery" section (8 placeholder photo tiles) between "Why Join" and the CTA banner — horizontally scrollable on mobile, 4-column grid on desktop, via an `isMobile` state driven by a `window.innerWidth < 720` resize listener; the header nav links and "Vendor Log In" button now hide under that same 720px breakpoint, leaving just the logo + "Apply as a Vendor"; the footer logo dropped its white card background and got bigger (73px). Also fixed the CTA section having no top padding (`0 24px 80px` → `80px 24px`), which made the gradient card sit flush against the gallery section instead of being evenly centered.
- **Hero section wired to admin Content tab (2026-07-13, first section of a section-by-section Content-tab redesign — see Admin Console → Content below).** Previously the hero's title, subtitle, and stat badges were hardcoded JSX strings, completely disconnected from `state.content` even though an admin "Content" editor already existed (it edited unused `title`/`subtitle`/`purpose` fields that nothing on the page read). Now `PublicHome.jsx` reads `content.heroTitle`, `content.heroSubtitle`, `content.heroImage`, `content.heroTag1`, `content.heroTag2` from the store. The title is stored as plain text; `heroTitleParts()` in `PublicHome.jsx` auto-highlights the substring "Sulap Artisan" (gradient text) wherever it appears, so the brand accent survives admin edits without needing a rich-text field. `heroImage` is `null` by default (renders the original gradient-circle placeholder) or a data URL from an admin photo upload (replaces the circle with a real `<img>`, `object-fit: cover`).
- **"Coming Soon" carousel wired to admin Content tab (2026-07-13, second section of the redesign).** The 4 event cards (day, month, name) were a hardcoded array in `PublicHome.jsx`, and the section heading was a literal `<h2>COMING SOON</h2>` — neither read from the store. Now both live on `content`: `content.comingSoonHeading` (the "COMING SOON" title) and `content.comingSoonEvents` (array of `{id, day, month, name, image}`, still fixed at 4 fixed-id entries — no add/remove UI, matching the deliberate "static placeholder, not the live events store" design noted above). Each card falls back to the original brown gradient tile when `image` is `null`; an uploaded photo (data URL) renders as `background-image: cover` behind the existing dark bottom-fade overlay and text. **Recommended upload size: 800 × 1100px** (portrait, ~4:5.5 ratio — 2.5x the card's on-screen 320×440 display size, shown as a tip in the admin editor itself).
- **"Why Join" section wired to admin Content tab (2026-07-13, third section of the redesign).** The 2×2 photo grid (4 plain color rectangles, no real images), the "Why join Sulap Artisan?" heading, subtitle, and the 4 numbered reason cards were all hardcoded in `PublicHome.jsx`. Now on `content`: `whyJoinTitle` (plain text, "Why join" auto-highlighted via the same `highlightPhrase()` helper the hero title uses — generalized from the old hero-only `heroTitleParts()`), `whyJoinSubtitle`, `whyJoinImages` (4 fixed-id tiles, each optional photo, falling back to the original alternating `#E8D3B4`/`#DCC49C` fill), and `whyJoinItems` (4 fixed-id `{title, body}` reason cards). Each grid tile keeps its own distinct corner radius (the original design's varied rounding per position) via a static `whyJoinTileStyle` layout array in `PublicHome.jsx` — that's shape/position, not editable content. **Recommended photo size: 600 × 600px** (square) — tiles render at a fixed 220px height inside a responsive 2-column grid, so a square crops cleanly at any column width.
- **"Our Gallery" and CTA banner wired to admin Content tab (2026-07-13, fourth/fifth sections of the redesign, done together).** Gallery's 8 tiles were a static `galleryTiles = [1..8]` array rendering plain dark-gradient boxes with no heading source; the CTA banner's title/subtitle were hardcoded strings. Now: `content.galleryHeading` + `content.galleryImages` (8 fixed-id tiles, optional photo each, falls back to the original dark gradient — **recommended 600 × 700px**, portrait ~6:7, matching the tile's 300×280 on-screen shape) and `content.ctaTitle` + `content.ctaSubtitle` (plain text, no photo slot — the CTA card itself is a fixed brand-gradient background, unlike the other sections). The CTA's two buttons ("Apply as a Vendor" / "Vendor Log In") stay code-driven, same as every other section — only copy is editable, not calls-to-action or navigation.
- **Footer wired to admin Content tab (2026-07-13, sixth/final section of the redesign).** The description paragraph, "Visit Us" address, and copyright line were hardcoded in `PublicHome.jsx`. Now on `content`: `footerDescription`, `footerAddress` (multi-line textarea — each `\n` renders as its own `<br/>`-separated line, same 3-line layout as before), and `footerCopyright`. The logo image and the "Vendors" column's 4 nav links (Apply as a Vendor / Vendor Log In / Coming Soon / Admin) were deliberately **left out of the editor** — they're site branding and in-app navigation actions, not editable copy, consistent with how the CTA's buttons were treated. This completes the section-by-section Content tab redesign — every home page section (Hero, Coming Soon, Why Join, Our Gallery, CTA, Footer) is now live-editable from Admin Console → Content.
- **Fixed: empty placeholder tiles disappeared entirely in Coming Soon, Why Join, and Our Gallery (2026-07-13, regression from the redesign above, same day).** Each tile's style object set both the `background` shorthand (for the gradient/color fallback) *and* the `backgroundImage` longhand (for an uploaded photo, else `undefined`) side by side. React writes `undefined` style values as an empty string, and setting `style.backgroundImage = ''` *after* `style.background = '<gradient>'` (object key order = the order React applies them) clears the image the shorthand had just set — so with no photo uploaded (the default, untouched state), every tile rendered as a fully invisible box instead of falling back to its gradient/color placeholder. Fixed by dropping the `background` shorthand everywhere and driving both the fallback and the photo through `backgroundImage` alone (`url(...)` or a CSS gradient function both work there); Why Join's solid-color fallback moved to `backgroundColor` instead, which doesn't conflict with `backgroundImage`. Verified via computed styles in-browser (fresh tab, no HMR/cache) that all three sections now show their fallback correctly with no photo set.

## Design & theme
- **Apple-style motion & material pass across both portals (2026-07-14).** Motion foundations in `index.css` (two shared curves: `--ease-spring` ≈ critically-damped settle for UI, `--ease-sheet` = Apple's sheet curve for the drawer): **(1) Press feedback** — every `button` scales to 0.97 on `:active` with a near-instant press and eased release (feedback on pointer-down, not release). **(2) Tab transitions** — both dashboards wrap their tab content in a `<div key={tab} className="tab-panel">`, so switching tabs remounts the wrapper and plays a 0.3s fade-up (`tabIn`). **(3) Translucent chrome** — the mobile menu trigger bars (`.vendor-tabs-bar`/`.admin-tabs-bar`) are now sticky with `backdrop-filter: blur(18px) saturate(160%)` over a 72% `color-mix` of `--bg-card`, so content scrolls under them. **(4) Card hover lift** — `.admin-cards`/`.event-cards` children lift 2px with a soft shadow on hover (pointer devices only, `@media (hover:hover)`). **(5) Modals** — the shared `Sheet`, `PayModal`, and `Toast` scale-fade in on the spring curve with a separate scrim fade (`scrimIn`). **(6) Drawer gestures** (`MobileNavDrawer.jsx`) — the drawer enters/exits along the same path (slides left, scrim fades with its progress), and supports **swipe-to-close**: 1:1 finger tracking after a 10px hysteresis threshold (taps still work), rubber-band resistance when dragged the wrong way, and a release decision based on **velocity** (fast left flick closes from anywhere; slow release only closes past 40% width) with the exit animated from the current position. **(7) Reduced motion** — `@media (prefers-reduced-motion: reduce)` redefines the movement keyframes as plain cross-fades (and stills the parking-pass pulse); the drawer's JS close path also skips its slide. `prefers-reduced-transparency` makes the sticky bars solid. All animation is compositor-friendly (`transform`/`opacity` only).
- **Site-wide re-theme 2026-07-12**, driven by the same Claude Design handoff: rose (`#A6364E`) + `Playfair Display`/`DM Sans` → warm amber/gold (`#9A5B26`/`#B97434`/`#7A431A`) + `Marcellus`/`Karla`, across every screen (public site, vendor portal, admin dashboard incl. dark/night mode, PDF exports).
- `Marcellus` only ships a single (400) weight — headings that used to be bold Playfair are now `fontWeight: 400` Marcellus, matching the design's own convention.
- CSS custom properties in `src/index.css` (`--bg-*`, `--text-*`, `--border-*`) were remapped to warmer equivalents; the semantic status-tint variables (`--tint-green-*`, `--tint-red-*`, `--tint-amber-*`, `--tint-blue-*` — used for approved/rejected/pending/etc. badges) were left untouched on purpose, since they encode status, not brand color, and happen to not collide with the new brand hex values.
- `App.css` (an unused create-vite scaffolding leftover, never imported) was deleted as part of this pass.
- **Dark-mode input text fix (2026-07-13).** Browsers don't inherit `color` into `<input>`/`<textarea>` by default, so every text field in the admin console rendered with the default black text — invisible against the dark `--bg-card` in night mode, even though the field's border and background did switch correctly. Fixed by adding `color:'var(--text-primary)'` to the shared `inp` style constant (`AdminDashboard.jsx`, used by ~27 fields across Content, Events, and other tabs) plus the handful of one-off styled inputs that had the same gap (Compliance's "new offence type" field, Admin Roles' "create admin" fields). **Deliberately left unfixed:** inputs inside popup/modal dialogs (e.g. the Categories "Add/Edit Category" modal) — out of scope per this pass, modals may need their own separate pass later.

## Portal navigation (2026-07-13 redesign)
- **Sign-in screens (`VendorLogin.jsx`, `AdminLogin.jsx`) no longer render the Sidebar/BottomNav shell.** `App.jsx` now detects `isSignIn` (`vScreen==='login'` or `aScreen==='login'|'reset'`) and routes straight to a new shared `AuthLayout.jsx` — a centered card on a full-bleed radial-gradient background (same phone-frame bypass pattern the public home page already used), instead of the old cramped-in-`.app-shell` layout. Previously the sign-in forms sat inside the same fixed-width phone frame as every other screen, with the Sidebar visible (and empty of anything useful) behind them.
- **"Back to home" button** is built into `AuthLayout.jsx` itself (top-left, fixed position) so both vendor and admin sign-in carry it automatically — this is also the fix for "signed-out users need a way back to the public site": Sign out (`VendorDashboard.jsx`/`AdminDashboard.jsx`) already dropped users on the login screen, which previously had no home link at all.
- **`AuthLayout`'s `dark` prop follows the real admin `darkMode` toggle** (`view==='admin' && darkMode`), not a hardcoded true — so admin sign-in respects day/night mode the same way the rest of the console does, and text colors (which use `var(--text-primary)` etc.) stay correctly paired with the card background instead of risking the same invisible-text bug fixed above.
- **Vendor "Forgot password?" flow added (`VendorLogin.jsx`)** — vendor login previously had no reset path at all (unlike admin, which already had a `mustReset` first-sign-in flow). It's a 3-state mock (`login` → `forgot` → `sent`): clicking "Forgot password?" next to the password field swaps to an email-entry screen, submitting shows a "Check your email" confirmation. **No real email is sent** — this matches the app's existing "Phase 1, mock data only" pattern (see admin's own "Forgot passwords are reset to the default by a super admin" messaging), it's a placeholder for a real reset flow once Supabase auth lands.
- **Sidebar's "NAVIGATE" section (Home / Apply as Vendor / Vendor Portal / Admin Console) is now hidden once inside a portal** (`Sidebar.jsx`, `!isVendor && !isAdmin` guard) — previously it showed above the contextual "MY PORTAL"/"CONSOLE" tab list even while already inside that exact portal, duplicating the "Vendor Portal"/"Admin Console" entry as a permanently-active, functionally dead link. It still shows on the vendor registration screen, where there's no portal context yet and the 4-item nav is the only way to move between Home/Apply/Portal/Admin.
- **Mobile tab navigation replaced: wrapping pill row → slide-in drawer.** `VendorDashboard.jsx` (10 tabs) and `AdminDashboard.jsx` (up to 18 tabs, role-filtered) used to render every tab as a `flex-wrap` pill inside `.vendor-tabs-bar`/`.admin-tabs-bar` — fine at 3-4 tabs, but wrapped into 3-5 cramped rows once the full list showed. Both now render a single full-width "current tab" trigger button (hamburger icon + active tab label + chevron) in that same CSS-class slot (still `display:none` ≥768px, since desktop uses the Sidebar), which opens a new shared `MobileNavDrawer.jsx` — a left slide-in panel (`drawerIn` keyframe, `src/index.css`) listing every tab, closes on selection or backdrop tap. Admin's drawer is always dark-themed (`dark` prop hardcoded true), matching the Sidebar's own admin styling being fixed dark regardless of the day/night toggle.
- **New icon:** `menu` (hamburger) added to `Icon.jsx` for the mobile drawer trigger.
- **Vendor registration also moved into `AuthLayout` (same session, follow-up).** The 4-step register form was the last screen still rendering inside the phone-frame shell with the Sidebar (and its NAVIGATE section) beside it. Now `App.jsx` routes `vScreen==='register'` through the same sidebar-less `AuthLayout` as the sign-in screens, with a wider card (`maxWidth` prop, 780 vs the sign-in screens' 420) to fit the form's 2-column desktop grid. The card now uses auto vertical margins instead of flex centering — centered when it fits the viewport, top-anchored and scrollable when taller (plain `align-items:center` would clip the top of the long form). With this, the Sidebar's NAVIGATE section has no remaining screen to show on — `Sidebar` now only ever renders the contextual MY PORTAL/CONSOLE tab lists inside the two dashboards.
- **Portal tab order is a super-admin global setting (2026-07-13, same session — reworked from an earlier per-user version the same day).** Initially each user could drag-reorder their own sidebar/drawer tabs; the user clarified the intent: **one arrangement, set centrally, applies to everyone.** Now the only reorder UI lives in **Admin Console → Settings → "Portal tab order"** (rendered for super admins only — staff admins see just the three toggles), with two cards: "Admin console tabs" (full `ADMIN_TABS` list incl. the super-only Admin Roles — a staff admin's RBAC-hidden tabs keep their assigned position) and "Vendor portal tabs" (`VENDOR_TABS`). Each card's rows can be dragged (dragged row dims, drop target shows an amber insert line) **or** nudged with per-row up/down arrows (touch-friendly), plus a "Reset to default" link that reverts to the code-defined order. The arranged order (`vTabOrder`/`aTabOrder` in the store, arrays of tab ids, `null` = default) is what every admin's sidebar/drawer and every vendor's portal render via `orderTabs()` (`helpers.js`; ids missing from a saved order — e.g. tabs added later in code — keep their code position at the end); vendors and staff admins have **no reorder controls anywhere**. `vTabOrder`/`aTabOrder` are also in `EDIT_SET_KEYS` so a view-only staff admin can't change them programmatically. **Mock-data caveat:** the order persists to localStorage (`sulap_vtab_order`/`sulap_atab_order`, same mechanism as dark mode) — a real backend is what would make it genuinely global across devices; on this mock build it's global across all roles *on the same browser*. Also from this work: `VENDOR_TABS` was deduplicated to live only in `VendorDashboard.jsx` (exported, same pattern as `ADMIN_TABS`); `Sidebar.jsx` imports it.
- **`BottomNav.jsx` deleted (same session, follow-up).** The mobile Home/Apply/Portal/Admin bottom bar became fully redundant: inside a portal the drawer + Sign out cover navigation, sign-in screens use `AuthLayout`, the public home renders standalone, and the register screen already hid it before this redesign — so no screen rendered it anymore. Removed the component, its `App.jsx` usage (`showNav`), and the `.bottom-nav` desktop-hide rule in `index.css`.

## Vendor side

### Vendor Registration (`VendorRegister.jsx`)
4-step form: business details + category + password → contact/social/logistics → product photos → review & accept market terms.
- **Fully wired (as of 2026-07-11):** all fields are controlled, bound to `state.rf`. Step 1 validates business name/owner/email/phone, category selection, and an 8+ char password before advancing. Submitting on step 4 requires terms acceptance, then dispatches a new vendor record (`status: 'pending'`, `regDate` = today) into the store — it shows up immediately in the admin's Vendor Applications queue. Photo upload is a mock tile counter (up to 8), not real file upload.
- **Step 1 field relabeled (2026-07-12):** "Owner name" → "Contact person (same as NRIC)" — data key is still `owner`, only the label/placeholder changed.
- **TikTok field icon fixed (2026-07-12):** the social-media row for TikTok used a generic camera icon (`Icon name="camera"`); now uses a proper `tiktok` glyph (`Icon.jsx`).
- New vendors are seeded with an empty `einvoice` record (see rule 16) — filled in later from the Profile tab once approved.
- Still mock/local only — nothing is persisted server-side yet (no Supabase), so registrations don't survive a page refresh.

### Vendor Login (`VendorLogin.jsx`)
Mock login — no real auth yet, always resolves to a hardcoded "current vendor" (`CURRENT_VENDOR_ID = 'v1'` in `mockData.js`) rather than checking a real password. **Status-gated (as of 2026-07-12):** login now checks that vendor's registration `status` before entering the dashboard — `pending` shows "still under review", `rejected` and `suspended` show their own blocking messages naming the reason, and only `status === 'approved'` reaches the dashboard. This is the mock stand-in for "vendor gets an email invite to log in once approved" — no real email is sent (see Known gaps), but the access gate itself is real.

### Vendor Dashboard (`VendorDashboard.jsx`) — 7 tabs
| Tab | What it does |
|---|---|
| Available Markets | Browse events, apply to a market. **Rule: a vendor can only apply if their registration `status === 'approved'`** — pending/suspended vendors see "Awaiting registration approval" and can't click through. **Second gate added 2026-07-12:** even once approved, the vendor must have completed their E-Invoice & bank details (see Profile tab below and rule 16) — until then the button reads "Complete E-Invoice info to apply" and is disabled. Applying is also blocked once an event's `lastApp` date has passed, or if the vendor already applied to that event. |
| My Applications | This vendor's event applications and their status |
| Documents | Vendor's on-file info (read-only, admin-managed) |
| Payments | Rental payment status per **approved** event application only. **Upload wired (as of 2026-07-11):** vendor can upload their own payment advice (toggle button — mock, no real file); once admin marks that application `partial`, a second "Upload second payment advice (remaining balance)" button appears. If the vendor is overpaid (admin recorded more paid than the total due), a red banner shows the overpaid amount and the refund status admin has set (in progress / completed with ref code + date + time), until admin closes the case. |
| Parking | **Rebuilt 2026-07-14 as a digital bi-fold Parking Pass, one per event day (see rule 22).** For each approved event application, renders a `ParkingPassCard` per day, **today's card sorted first**: only today's card is foldable — it shows a live (pulsing) countdown to that day's fixed 11:59PM cutoff once admin has entered a serial, or a pulsing "AWAITING PARKING DETAILS" before that — while past days show a greyed-out "EXPIRED" card and future days a greyed-out "LOCKED" card, both closed and non-interactive. |
| Vendor Pass | **Rebuilt 2026-07-13 as a digital pass application flow (replaces the old physical tag collection/return tracking entirely — see rule 21).** For each approved event application (gated one-event-at-a-time, rule 21), the vendor applies with up to `PASS_SELF_SERVICE_MAX` (3) pass holders (name + photo each) — a "+ Add another pass holder" link reveals the 3rd slot on the initial form, no admin action needed to reach it. **Each pass holder is approved/rejected individually**, and once approved renders as its own bi-fold flip digital pass card (`components/DigitalPassCard.jsx`, tap to unfold and reveal the photo, booth number shown on the folded face). **Beyond 3 is admin-initiated only (2026-07-13, third pass)** — there's no vendor-side "request more passes" flow anymore; admin grants extra slots directly from their side, and the vendor just sees new slot(s) to fill in once granted. Photo upload offers both "upload from gallery" and "take a photo" (camera capture), behind a notice that only the actual person attending should be photographed. Any pass holder — approved or rejected — can be individually **edited and resubmitted** (only that person goes back to `pending`, the vendor's other pass holders are untouched). A "Reset my Vendor Pass (testing)" link clears the application for a fresh run-through of the whole flow. |
| Profile | Vendor's own registration details — **expanded 2026-07-12**, see rules 16–17 below. Three cards: "Vendor details" (locked, request-based), "Social media & power supply" (directly editable), "E-Invoice & bank details" (locked, request-based, required before applying). |

### Booth sharing rule (apply flow, `Modals.jsx` → `ApplyModal`)
When a vendor applies to an event, they choose solo or shared booth:
- A shared booth can have **up to 3 vendors total** (the applicant + up to 2 partners)
- Booth partners must be the **same tier** — an F&B vendor can only share with other F&B vendors, and a non-F&B vendor only with other non-F&B vendors
- **Booth partners must be `approved` vendors** (fixed 2026-07-11 — the partner search previously let `pending`/`suspended` vendors show up and be picked; now filtered out alongside the tier check)
- Partners are picked by searching existing registered vendors (live search-as-you-type on business name, up to 5 results)

## Admin Console (`AdminDashboard.jsx`) — 16 tabs

| Tab | What it does |
|---|---|
| Overview | Stat tiles (total vendors, pending review, active events, payments confirmed) + quick actions |
| **Vendor Applications** | **New sign-ups only** (`status === 'pending'`). Approve/Reject here. Once decided, the vendor drops off this list. |
| **Vendor Listing** | Master list of vendors that have been decided on — `status === 'approved'` or `'suspended'`. Shows each vendor's **compliance tags** (offense counts by type, or "No offences on record"). This is the vendor base eligible to apply for events. **Vendor detail modal expanded (2026-07-12):** now has three admin-editable sections, each with its own "Edit" toggle that saves straight to the vendor record (`MERGE_VENDORS`, logged) — **admin edits are final and instant, unlike the vendor-side request flow (rule 17)**: "Vendor details" (brand, contact person, category, email, phone, car plate, product description), "Social media & power supply" (Instagram/Facebook/TikTok/power), and (for approved vendors) **E-Invoice & bank details** with a "Complete"/"Incomplete" badge. If the vendor has a pending change request (rule 17), it's shown inline at the top of the modal with an old → new diff and Approve/Reject buttons. **Sign-up form downloads (added 2026-07-12):** each vendor card has a "Sign-up form" button that downloads that vendor's registration & T&C acceptance form, and a "Download all forms" button next to Export CSV bulk-downloads every listed vendor's form as one ZIP (respects the current search filter; falls back to the full list if the search has no matches). See rule 18. |
| **Profile Requests** (added 2026-07-12) | Queue of vendor-submitted requests to change locked profile fields — see rule 17. Each pending card shows the vendor, which section (`details` or `einvoice`), and an old → new diff of only the fields that actually changed, with **Approve** (applies the change to the vendor record), **Reject** (discards it, vendor stays as-is), and **View vendor** (opens the same detail modal as Vendor Listing). Decided requests drop off the pending list into a collapsed "Show N decided requests" section, matching the Vendor Applications rejected-list pattern (rule 7). |
| Events | **Redesigned 2026-07-15 (rule 24, supersedes rule 23), fixed 2026-07-16 (rule 25).** Create Event is a 4-step wizard (Picture → Details → Schedule → Pricing) side-by-side with Existing Events, a horizontally-scrolling rail of portrait cards (no longer layered/overlapping — see rule 25). Same underlying fields as before: name, photo, dates, times, F&B/Non-F&B daily rate + auto 6% SST, last-apply-by date. A search icon on the rail filters existing events by name. **Editing wired (as of 2026-07-11):** clicking an existing event card (or its pencil button) opens a centered "Edit event" modal (name, location, photo, dates, times, last-apply date, rates) prefilled from that event, with a live duration + rental-total preview matching the create form. Saving recomputes `dateRange`/`days` and updates the event in place. **Event image is a real photo upload** (as of rule 25) — `<input type="file">` → data URL, stored as `img: url("...") center/cover no-repeat`; a fixed gradient (`EVENT_IMG_PALETTE[0]`) is the silent fallback when no photo has been uploaded. |
| Event Applications | **Two sub-tabs (as of 2026-07-12, superseding the old single-list flow described below through several sessions): Applications and Shortlist.** **Applications** lists vendors applying to the event filtered by the dropdown, `status === 'pending'` only, each showing up to 3 compliance tags and a **Shortlist** button (`status → 'shortlisted'`) plus "View & share booth". **Shortlist** is the final roster for that event — `status === 'shortlisted'` or `'approved'` vendors, grouped by category, sorted shortlisted-first. Shortlisted vendors get **Approve** (`→ 'approved'`) and **Reject** (`→ 'pending'`, moves them back to Applications) buttons, unless they're on a compliance hold (see rule 13) in which case Approve is replaced by **Override hold**. Approved vendors show a faint "Approved" label and a **Release vendor** link (see rule 14, added 2026-07-12). **Booth partner profiles wired (as of 2026-07-11):** clicking a partner row in the "View & share booth" detail opens that vendor's full profile (same modal as Vendor Listing), on top of the same tab; closing the profile returns to the application detail instead of just closing outright. **Main applicant's own full profile now linked too (added 2026-07-13, rule 15):** previously only booth partners had this link — a solo-booth application had no way to see the applying vendor's full details (contact, e-invoice/bank info, etc.), only the summary (name, description, photos) shown inline in the application sheet. **Desktop layout is a 2-column grid** (see business rule 10) instead of one long single-column list. Page size bumped from 15 to 20 (`PER_PAGE` in `store.jsx`, shared across every paginated admin list, not just this tab). |
| Payments | Per approved event application: rental subtotal + 6% SST + RM100 refundable deposit (only added **if that vendor's deposit isn't already paid** — see Deposit Record rule below) = total due. Send a payment reminder, or remove a vendor from the event (releases their slot — different from closing a refund case, see rule 11). **Document directions fixed (as of 2026-07-11):** Payment advice is **vendor-uploaded** — admin only downloads it (shows a faint "not yet uploaded by vendor" label until the vendor has one on file; no admin-side toggle since admin can't upload it themselves). Invoice and Receipt remain **admin-uploaded**, unchanged toggle behavior. **Per-market export added (as of 2026-07-11):** an Export button next to the event filter exports the currently-filtered market's payment data (mock — shows a toast, not a real file yet). **Second payment advice + overpayment/refund flow added (as of 2026-07-11):** see business rule 11 below. **"Mark Paid/Partial/Unpaid" replaced with "Record payment" (as of 2026-07-11):** see business rule 12 below. |
| Deposit Record | RM100 refundable deposit — **tracked once per vendor (not per event), and tied to event participation, not registration.** If unpaid, it's automatically folded into that vendor's *first* event invoice (see `payCalc` in `helpers.js`). Statuses: unpaid → paid → refunded. |
| Parking | Per-day parking **serial** numbers (relabeled from "ticket" 2026-07-14 — see rule 22) for approved applicants of the selected event; this is the only field admin fills in to produce a vendor's digital Parking Pass. **Rule: entry fields are locked (read-only) unless today's date falls within the event's start/end dates** — admin can manually "Override" the lock if needed. |
| Event Pictures | Vendor-uploaded product photos (read-only count) + admin can upload additional event photos per vendor, and download a vendor's uploads |
| Vendor Pass | **Rebuilt 2026-07-13 (see rule 21).** Select an event (grouped-by-event, same pattern as Payments/Parking), with an **Export report (PDF)** button next to the picker (2026-07-13, third pass — see rule 21) that downloads a landscape PDF of every *approved* pass holder for that event: vendor name, photo, pass holder name, booth number, event date. Below that, review each vendor's pass application: a **Booth number** field (free text, e.g. "A12") that flows straight onto that vendor's digital pass cards, and each pass holder shown individually with its own name + photo thumbnail and **Approve/Reject** — not one decision for the whole application. Rejecting opens a reason picker (photo unclear/blurry, not a real photo of a person, face not visible, name/photo mismatch, duplicate person, or a free-text "other") that's stored on that pass holder and shown back to the vendor. An **Add pass** button (2026-07-13, third pass) lets admin directly grant a vendor extra pass slots beyond their self-service default of 3 — admin picks the count, no vendor request involved; the vendor just sees the new slot(s) appear to fill in. **Photo preview added 2026-07-13:** click any pass holder's thumbnail to open it full-size in a modal, with a Download button — works regardless of that person's status. |
| Categories | **Expandable accordion cards (redesigned 2026-07-12).** Each category shows icon + name + full description + member count; click to expand/collapse and see vendors nested inside. Admin can add categories (via modal: name + description + icon picker) or remove categories (with confirmation). Vendors are reassignedto different categories via inline dropdown. Search box filters vendors across all categories. All categories now pull from store (`INITIAL_CATS`, `cats` state), so new categories added in admin automatically appear in vendor sign-up registration form. |
| Activity | **Live log (as of 2026-07-11).** Real entries are appended via a `logActivity()` store helper on: vendor approve/reject/suspend/reinstate/reconsider, event application approve/reject, event creation, category add, offence logging, content updates, deposit record updates, and vendor-side registration/event-application submissions. Newest first, filterable by All/Admin/Vendor. Seeded with 5 historical entries on first load. |
| Vendor Chart | Vendor performance/ranking view |
| Compliance | Two sub-tabs: **Log offences** (pick an event, tag vendors with an offense type — only vendors with an *approved* application to that event are selectable) and **Vendor review** (per-vendor offense history, grouped by type with counts — this is the same data source that powers the compliance tags shown in Vendor Listing and Event Applications) |
| Content | **Section-by-section redesign completed 2026-07-13** to match the re-themed public home page — the old editor (badge/title/subtitle) was disconnected from what the page actually renders; every section below is now live-wired to `PublicHome.jsx`. **Hero:** title (plain text, "Sulap Artisan" auto-highlighted), subtitle, hero photo (upload/remove, falls back to the gradient-circle placeholder), and the two floating stat badges. **Coming Soon carousel:** section heading + each of the 4 event cards' name/day/month/photo (800×1100px recommended, falls back to the gradient tile). **Why Join:** title ("Why join" auto-highlighted), subtitle, 4-tile photo grid (600×600px recommended, falls back to alternating color fills), and the 4 numbered reason cards' title/description. **Our Gallery:** section heading + 8 photo tiles (600×700px recommended, falls back to the dark gradient tile). **CTA banner:** title + subtitle (plain text, no photo slot). **Footer:** description paragraph, "Visit Us" address (multi-line), and copyright line — logo and nav links stay code-driven. **Market terms & conditions** (shown at vendor registration step 4) upgraded from a plain textarea to a WYSIWYG rich-text editor (2026-07-13, see rule 20) — headings, bold/italic/underline, and ordered/bullet lists, for the long formatted policy text this field holds. |
| Settings | Three toggles: **Auto-approve vendors** (wired as of 2026-07-11 — when on, registration submits directly with `status: 'approved'` instead of `'pending'`, skips the Vendor Applications queue, and shows a "You're in!" confirmation instead of "we'll review it"), **Show events publicly**, **Email alerts** (wired as of 2026-07-11 — when on, vendor status-change toasts append "· vendor emailed"; no real email is sent, this is still a mock signal, not an actual notification system) |

---

## Compliance / offense types (`OFFENSE_TYPES` in `mockData.js`)
Fixed list admins tag vendors with: Late opening, Early closing, Late payment, Poor booth cleanup, No-show / last-minute withdraw, Unsanctioned selling. Each has its own color used consistently across Compliance, Vendor Listing, and Event Applications tags.

## Key business rules

1. **Registration ≠ event approval.** A vendor's `status` (`pending` → `approved`/`rejected`, or `approved` → `suspended`) only reflects whether they're allowed to *be* a Sulap vendor at all. It says nothing about payment or event participation. An approved vendor's individual event applications still each need their own admin decision — event application status (`pending`/`approved`/`rejected` on the application record) is a **separate status from vendor registration status**, even though both reuse the same three words. To keep them from being confused for each other (fixed 2026-07-11): the admin Event Applications list drops the badge entirely and uses buttons instead (see that tab's row above); the vendor's own My Applications tab labels its badge "Awaiting review" instead of "Pending" for the equivalent state.
2. **Vendor must be `approved` before they can apply to any event, or appear in booth-sharing search.** A vendor still sitting in the Vendor Applications queue (`pending`) has zero event-side visibility — no Apply access of their own, and other vendors can't find or add them as a booth partner. Enforced in `VendorDashboard.jsx` (apply gate) and the partner search in `Modals.jsx` → `ApplyModal` (`v.status === 'approved'` filter, fixed 2026-07-11 — see mock data note below).
3. **Deposit and payment status belong to event participation**, not registration — they must never appear on the registration review screen (Vendor Applications / Vendor Listing "view details" modal).
4. **Deposit is per-vendor, not per-event**, and auto-attaches to the vendor's first unpaid event invoice.
5. **Booth sharing** is capped at 3 vendors and restricted to same-tier (F&B/non-F&B) partners.
6. **Parking serial entry is time-locked** to the event's actual dates unless manually overridden by admin.
7. **Suspend and Reject are both reversible.** Suspend only appears on already-approved vendors, is a small deliberately low-emphasis link (not a full button) behind a confirm dialog, and sets `status: 'suspended'` — vendor loses event-application access but stays visible in Vendor Listing with a "Reinstate vendor" option. Rejected vendors are hidden by default under a collapsed "Show N rejected applications" section at the bottom of Vendor Applications, with a "Reconsider" action that moves them back to `'pending'`.
8. **All popups/modals across both admin and vendor are centered dialogs**, not bottom sheets — as of 2026-07-13, `Sheet` (`components/Modals.jsx`, the shared wrapper behind every modal except the admin Category editor and `App.jsx`'s `PayModal`, both already centered) always centers; it previously defaulted to a bottom sheet unless a `centered` prop was passed, which every vendor-facing modal (Apply, Vendor Pass, Deposit, Refund, Doc Preview) omitted. Originally this rule only covered the admin-only vendor/event-application/event-edit modals — now it applies uniformly everywhere.
9. **Events close to applications automatically** once today's date passes the event's `lastApp` date — enforced both on the public home page and inside the vendor dashboard's apply button.
10. **Admin card lists are 2 columns on desktop, 1 on mobile** (`.admin-cards` CSS class, `AdminDashboard.jsx`, as of 2026-07-11) — applied to every top-level paginated/browsable card list in the admin console (Vendor Applications incl. its rejected sub-list, Vendor Listing, Events, Event Applications, Payments, Deposit Record, Parking, Event Pictures, Vendor Pass, Categories incl. "Vendors by category", Compliance's both sub-tabs). Deliberately *not* applied to the Activity feed (a chronological timeline with connecting lines — 2 columns would break reading order) or the Vendor Chart ranking (a single card with an internal ranked list, not a list of cards).
11. **Second payment advice + overpayment/refund flow (as of 2026-07-11).** When admin marks a vendor's event payment `partial`, the vendor's Payments tab gains a second "Upload second payment advice (remaining balance)" button (`payments[key].advice2`) — admin can then download it too, mirroring the first. Separately, if a vendor is overpaid (`paid > total due`, computed live, not stored), both sides show an "Overpaid by RM X" banner. Admin gets an **Arrange refund** action (`RefundModal` in `Modals.jsx`) requiring reference code + date + time before it can be marked complete (`refunds[vendorId-eventId]`, `MERGE_REFUNDS`); once complete, admin gets a **Close case** button. The vendor sees the same refund status read-only (in progress → completed → closed) so both sides always agree on where the refund stands — closing doesn't delete the record, it just stops showing the action buttons and switches to a muted "Refund closed" line on both sides.
12. **Payment status is derived from an amount, never asserted directly (as of 2026-07-11).** The old "Mark: Paid / Partial / Unpaid" buttons let admin flip the status label without ever recording how much was actually received (the standalone "Paid" button didn't even open the amount modal). Replaced with a single **Record payment** button (pre-filled with the outstanding total for a one-click exact match) that always takes an amount and derives status: amount = total → `paid`; amount < total → `partial`; amount > total → `paid` + triggers the overpaid/refund flow (rule 11); amount = 0 → `unpaid`. A separate **Reset to unpaid** button handles corrections without opening the modal. This is the actual live `PayModal`, defined in `App.jsx` — a near-identical but unused duplicate `PayModal` in `Modals.jsx` was deleted in the same pass (dead code that had silently drifted out of sync with the one actually rendered).
13. **Compliance hold blocks Approve on the Shortlist tab.** A shortlisted vendor with unresolved offences in the last `skipN` markets (settings-configurable "skip after N offences" window) cannot be approved directly — `complianceHold(vendorId, eventId)` in `AdminDashboard.jsx` returns their matching offences, and if any exist and no override is on file (`compOverrides[vendorId-eventId]`), the row shows an amber hold banner and swaps the Approve button for **Override hold**, which sets `compOverrides[...] = true` (logged + toasted) and unlocks the normal Approve button — it does not clear or resolve the underlying offences, only permits this one approval.
14. **Release vendor (added 2026-07-12) reverses an approval, same target state as Reject.** Once an application reaches `status === 'approved'` on the Shortlist tab, no other action existed to undo it (a prior session's fix, commit `f71666b`, intentionally removed the old "Remove" button so approved vendors "stay" in the Shortlist as the record for that market). For last-minute vendor cancellations, a **Release vendor** text link is available — deliberately low-emphasis (underlined text, no background, red tint) rather than a full button, matching the existing Suspend-vendor pattern (rule 7), since this is a rare, consequential action that shouldn't visually compete with the primary Approve/Reject flow. **Positioned in the button row next to "View & share booth" (fixed 2026-07-12), not beside the name/Approved label** — an earlier placement wrapped inconsistently at narrow widths depending on vendor name length and whether the "Sharing" tag was present. Behind a `window.confirm` guard naming the vendor; on confirm it sets `status: 'pending'` — identical destination to the shortlist Reject action — so the vendor reappears in the Applications tab exactly as if never decided, rather than being deleted or left in limbo. Logs via `logActivity` and shows a toast, matching every other status transition in this tab.
15. **Application detail modal (`AppDetailModal` in `Modals.jsx`) now links to the applying vendor's own full profile, not just partners' (added 2026-07-13).** Previously the booth-sharing section only made *partner* rows clickable through to their full `VendorDetailModal` profile (contact info, e-invoice/bank details, product photos, Suspend action, etc.) — the main applicant (`v`) had no equivalent link, so a solo-booth application showed only the inline summary (name, description, photos) with no way to reach the vendor's full record. Fixed by adding a clickable row directly under the status badge — same visual pattern as a partner row (icon + name + chevron on a tinted background) — that opens `vendorDetailId: v.id` with `vendorDetailReturnAppId: appDetailId` set, so closing the vendor profile returns to this application detail rather than closing everything. This applies uniformly whether the application is solo or shared; a shared application now shows both the main vendor's link (new) and the partner's link (pre-existing) side by side.

15. **Category data model (as of 2026-07-12): `{id, name, description, icon}`.** Previously `INITIAL_CATS` had only `{id, name}` (bare structure) while `VendorRegister.jsx` maintained its own hardcoded duplicate with full metadata. Now unified: a single `INITIAL_CATS` in mockData provides all fields. VendorRegister pulls `cats` from store instead of hardcoded; admin's Categories tab can add categories with the modal providing name + description + icon picker. All categories now sync across vendor sign-up and admin console — add a category in admin, it appears in the sign-up form immediately.
16. **E-Invoice & bank details are mandatory before a vendor can apply to any market (added 2026-07-12).** Once a vendor is `approved`, their Profile tab (`VendorDashboard.jsx`) gains an "E-Invoice & bank details" card — 8 required fields: Company name (per SSM), Company Registration Number (new, 12 digits), Tax Identification Number (TIN, no spacing), SST Number (or "N/A" if not SST-registered), Business registration address, and Bank Name / Account Number / Account Holder Name (for deposit refunds). `einvoiceComplete(vendor)` (`lib/helpers.js`) checks all 8 fields are non-empty on `vendor.einvoice`. The Available Markets apply button checks `vendorApproved` **and then** `einvoiceOk` — incomplete vendors see "Complete E-Invoice info to apply" instead of the normal Apply/Applications-closed states. Data lives on the vendor record as `vendor.einvoice = {companyName, regNo, tin, sstNo, regAddress, bankName, bankAccNo, bankHolder}`, seeded empty (`EMPTY_EINVOICE` in `mockData.js`) for new registrations. **`v1` (Nutmeg & Clay, the mock "current vendor") is seeded pre-filled** — it originally started empty to demo the gate live, but that was inconsistent with its own seed data already having an *approved* application to Tamu Weekend Bazaar (`INITIAL_APPS` entry `a2`), which shouldn't have been possible under this rule. The gate itself only applies going forward (it doesn't retroactively revoke pre-existing approvals), but the seed data now reads as compliant rather than contradictory. Borneo Brews and Pulau Soap Co. also carry pre-filled sample data; `v3`/`v5`/`v6`/`v7` remain empty to demonstrate the "Incomplete" state in the admin Vendor Listing modal. **As of the request-flow change (rule 17), the vendor no longer writes `vendor.einvoice` directly** — their submission goes through the same admin-approval queue as other locked fields, and `einvoiceOk`/the apply gate only reflect data admin has approved.
17. **Vendor Profile fields split into directly-editable vs. admin-approval-required (added 2026-07-12).** Only **Instagram, Facebook, TikTok, and Power supply needs** are directly vendor-editable — saved instantly via `MERGE_VENDORS` from the Profile tab's "Social media & power supply" card, no review needed. **Everything else vendor might want to change goes through a request queue**: the "Vendor details" card (Brand name, Contact person, Category, Email, Phone, Car plate number, Product description) and the "E-Invoice & bank details" card (all 8 fields, including a vendor's *first-ever* submission — see rule 16) both use a "Request a change"/"Complete now" flow. Submitting pushes a record onto `state.profileRequests` (`{id, vendorId, section: 'details'|'einvoice', changes, submittedAt, status:'pending'}`, `MERGE_PROFILE_REQUESTS` in `lib/store.jsx`) instead of writing the vendor record directly; the Profile tab shows an amber "pending admin review" banner and blocks a second request for that section until the first is decided. **Admin has two ways to act on this:** (1) a dedicated **Profile Requests** tab (queue view, approve/reject with an old→new diff) or (2) inline at the top of the Vendor Listing detail modal when opened for that vendor. Either path, on **Approve**, merges `req.changes` onto the vendor record (`einvoice` section nests under `vendor.einvoice`, `details` section merges flat) and marks the request `approved`; **Reject** just marks it `rejected` and the vendor record is untouched — both log activity and toast. **Admin's own edits bypass this queue entirely** — the same Vendor Listing modal has direct "Edit" toggles on all three sections (vendor details, socials & power, E-Invoice) that save straight to the vendor record with no approval step, since admin edits are already final.
18. **Downloadable vendor sign-up & T&C acceptance forms as PDFs (added 2026-07-12).** Every vendor record carries `tcAcceptedAt` ("14 Jun 2026, 10:24 AM" format) — recorded via `tcTimestamp()` (`lib/helpers.js`) at the moment the vendor submits registration with the terms checkbox ticked (the checkbox itself can only be ticked after scrolling the full terms), and seeded for all mock vendors. `lib/signupForm.js` generates a formal acceptance document per vendor: Sulap Artisan logo (fetched from `/assets/sulap-lockup.png` and embedded as a data URL), reference number (`SA-REG-<ID>`), all registration details (business details incl. product description, socials, power supply, photo count, E-Invoice & bank details or "not yet submitted"), the full market terms text (from `content.terms`, so the form reflects the terms as currently configured in the Content tab), and a highlighted **Declaration of acceptance** block naming the contact person + business with the exact date & time of acceptance. PDFs are generated client-side using `html2pdf.js` (lightweight, no server call needed; logo is embedded so files work offline). **Named after the vendor**: single download saves as `<Vendor Name>.pdf`; bulk saves `Sulap Artisan - Vendor sign-up forms.zip` containing one PDF per vendor. Both downloads log to Activity.
19. **Popup/scrollbar redesign (2026-07-13):** every scrollable popup (all `Sheet`-based modals in `Modals.jsx`, the admin Category editor, and the admin sidebar's tab-nav list) now uses a thin, theme-colored scrollbar (`.themed-scroll` / `.themed-scroll-light` in `index.css`, using `scrollbar-color`/`::-webkit-scrollbar-thumb`) instead of the browser's default gray one. Each modal's outer rounded box is `overflow:hidden` with the actual `overflow-y:auto` scroll region nested one level in — the outer box's `border-radius` now clips the scrollbar's corners, so it no longer squares off against the rounded modal edges (previously the scrolling element *was* the rounded box itself, so webkit didn't clip its own scrollbar to its own radius). `Sheet` (the shared wrapper behind every modal except the Category editor and `App.jsx`'s `PayModal`) also **always centers now** — see rule 8. `Sheet`'s background stays a fixed light cream (not the dark-mode `--bg-card`) because its internal content still uses hardcoded light-mode hex colors throughout; making the wrapper dark-aware without also reworking every color inside would leave text unreadable — flagged as a follow-up if full dark-mode modal support is wanted later. `.themed-scroll` (theme-aware, used by the Category modal and sidebar) vs. `.themed-scroll-light` (fixed light, used by `Sheet`) exist for exactly this reason — pick whichever matches the container's actual background.
20. **Market terms are rich HTML, edited via a WYSIWYG editor (added 2026-07-13).** `content.terms` was a plain-text template literal (headings were just numbered lines, no real formatting) edited in a plain `<textarea>`. Replaced with `components/RichTextEditor.jsx`, a themed wrapper around `react-quill-new` (Quill, React-19-compatible fork) with a toolbar limited to headings, bold/italic/underline, and ordered/bullet lists — enough for long formatted policy text without a full document-editor's surface area. `content.terms` now stores Quill's HTML output directly; `INITIAL_CONTENT.terms` in `mockData.js` was converted from the old plain-text template to equivalent HTML (`<p>`/`<strong>` per section). Every consumer of `content.terms` had to switch from treating it as escaped plain text to raw HTML: `VendorRegister.jsx` step 4 now renders it via `dangerouslySetInnerHTML` (previously a plain text node with `white-space:pre-wrap`) inside a new `.terms-content` class (`index.css`) that styles the headings/paragraphs/lists Quill produces; `lib/signupForm.js`'s PDF generator now inserts `terms` raw into the `.terms` div instead of running it through `esc()` (HTML-escaping it — which is correct for genuinely untrusted input, but `content.terms` is always admin-authored via the Content tab, never vendor-submitted, so this is safe) and gained matching `h2`/`p`/`ol`/`ul`/`li` styles so the PDF's terms section reflects the same formatting. **`content.terms` must stay admin-only content** — anything that ever let a non-admin write to this field would need HTML-escaping added back, since it's now rendered unescaped in two places.
21. **Digital Vendor Pass — application, per-person approval, self-service + admin-granted slots, booth numbers, PDF report, and one-event-at-a-time gating (rebuilt 2026-07-13, replacing the old physical tag collection/return tracking entirely; reworked twice more the same day — per-person approval, then self-service slots + admin grants + PDF export).** Built from a Claude Design handoff (`Sulap Artisan Landing Page Design-handoff.zip` → `Vendor Pass.dc.html`, a bi-fold flip card). Data model: `state.passApps` — one record per vendor+event, `{id, vendorId, eventId, extraApproved, boothNumber, people:[{id,name,photo,status:'pending'|'approved'|'rejected', rejectReason, decidedAt}], submittedAt}`. **There is no single application-wide status** — each pass holder in `people[]` carries its own `status`/`rejectReason`/`decidedAt`, decided independently. **`state.passRequests` no longer exists (removed 2026-07-13, third pass)** — there is no vendor-side "request more passes" flow; a vendor can self-service up to `PASS_SELF_SERVICE_MAX` (3, `mockData.js`) people with no admin action needed, and going beyond that is admin-initiated only via the "Add pass" grant (below). `PASS_REJECT_REASONS` (`mockData.js`) is the fixed list admin picks from when rejecting a person's photo: unclear/blurry, not an actual photo of a person, face not visible, name/photo mismatch, duplicate person already has an approved pass, or a free-text "other".
    - **Vendor side (`VendorDashboard.jsx` → Vendor Pass tab):** once an event application is `approved`, the vendor applies with up to `PASS_SELF_SERVICE_MAX` pass holders (name + photo each, at least 1 required) — the initial form shows 2 slots plus a "+ Add another pass holder" link to reveal the 3rd (`addInitialFormRow()`, capped at `PASS_SELF_SERVICE_MAX`). This submission creates the `passApp` with every filled person starting `status:'pending'`. **Fixed 2026-07-13, same day:** that link sat next to the "Submit" button on the same line on desktop only, because `.cta` (`index.css`, `@media (min-width:768px)`) forces the button to `width:auto !important` at desktop widths — an inline `<span>` link doesn't force a line break before an auto-width sibling the way it does when the button is still full-width on mobile. Changed the link to a block-level `<div>` so it always occupies its own row regardless of the button's computed width. A notice above every photo-submission point (`PassPhotoNotice`, a shared component) warns that only a photo of the actual person attending should be uploaded, since the pass is used for mall entry outside normal operating hours. Each person then renders according to *their own* status: `pending` shows a plain "awaiting admin review" row; `rejected` shows the admin's reason plus an "Update photo & resubmit" action; `approved` renders as a `DigitalPassCard` (bi-fold flip, tap to unfold to reveal the photo, booth number shown on the folded face as "No. `<boothNumber>`") with a "Need to change this pass holder? Edit" link underneath. If `maxSlots` (`PASS_SELF_SERVICE_MAX + extraApproved`) is reached, a muted note tells the vendor that going further is arranged through the Sulap team directly — no self-service path beyond that.
    - **Per-person edit, not whole-application cancel/reapply:** editing any one pass holder (`startEditPerson`/`submitPersonEdit` in `VendorDashboard.jsx`) reopens just that person's name+photo fields (local `editingPersonId`/`personEditForm` state, no store write until saved) and on submit resets **only that person** back to `status:'pending'` with a cleared `rejectReason` — every other pass holder on the same application keeps their own status untouched.
    - **Admin-granted extra slots, no vendor request (reworked 2026-07-13, third pass):** an **Add pass** button on each vendor's card in the admin Vendor Pass tab opens a small "how many additional pass slots to grant?" number field; confirming (`confirmAddPass()`) adds that count straight onto `passApp.extraApproved`, logged and toasted. The vendor never asks for this and never sees a pending-request state — the newly unlocked slot(s) simply appear as fillable ("Add pass holder N of maxSlots", using the same `submitExtraSlot()` flow as self-service slots), and go through the normal per-person approval once filled in.
    - **Admin side (`AdminDashboard.jsx` → Vendor Pass tab):** grouped by event via the same "Select event" dropdown pattern as Payments/Parking, with an **Export report (PDF)** button beside the picker (see below). Every vendor with a pass application for that event shows a card: a **Booth number** input (free text, dispatches straight to `passApp.boothNumber` on change, same "no separate save step" pattern as the Parking tab's per-day ticket fields), each pass holder listed individually with its own **Approve/Reject**, and the **Add pass** grant control described above. Rejecting opens an inline reason picker (`rejectingPersonId`/`rejectReasonKey`/`rejectReasonOther` local state) — selecting "Other reason" reveals a free-text box — and "Confirm reject" stores the chosen/typed reason on that person (`decidePerson`/`confirmReject`). Approving, rejecting, or granting slots for one vendor never touches any other pass holder or application.
    - **Vendor Pass PDF report (added 2026-07-13, third pass; layout redone the same day, fourth pass):** `lib/passReport.js` (`downloadPassReport(event, passApps, vendors)`) builds a **portrait** A4 PDF for the currently-selected event, **grouped by vendor** rather than one flat table — each vendor gets a single bordered box headed `<boothNumber> — <vendor name>` (booth number sorts the boxes down the page via `boothCompare()`, a locale-aware natural sort so "A2" comes before "A10"; boothless vendors sort last), containing all of that vendor's *approved* pass holder photos (100×100, uniform size — no more tiny thumbnails, no per-person "vendor name"/"booth"/"event date" columns repeating the same values). Reuses the same `html2pdf.js` + Sulap logo pattern as the vendor sign-up form (`lib/signupForm.js`, whose `getLogo()` helper is exported and shared). Saved as `<Event name> - Vendor Pass report.pdf`. Pending/rejected pass holders are intentionally excluded — the report footer says so — since this is meant to double as an at-the-door reference for the Sulap team / mall security, where only valid (approved) passes matter.
    - **One-event-at-a-time gate (unaffected by any of the reworks above):** a vendor's approved event applications are sorted by event start date into a chain. The vendor may only *start* a new Vendor Pass application for the next event in the chain once the previous event's `endDate` has already passed — enforced per-application, not globally. Events not yet unlocked show a "you can apply once `<previous event>` has passed" notice instead of the apply form. This gate is based purely on event dates and whether an application record exists yet.
    - **Removed:** the old `passModalVendor`/`PassModal` (`components/Modals.jsx`) physical-tag flow and its `state.passes`/`MERGE_PASSES` store plumbing (superseded 2026-07-13, first pass); the original app-wide `passApp.status`/`decidedAt` fields and the whole-application "Cancel & reapply" flow (superseded the same day, second pass, by per-person status + per-person edit); `state.passRequests`/`MERGE_PASS_REQUESTS` and the vendor-initiated "request additional passes" flow entirely, including its admin-side approve/reject card (superseded the same day, third pass, by self-service up to `PASS_SELF_SERVICE_MAX` + admin-granted `extraApproved`).
    - **Admin photo preview (added 2026-07-13):** clicking a pass-holder thumbnail opens `PassPhotoPreviewModal` (`components/Modals.jsx`, `state.passPhotoPreview = {name, photo}`) — a full-size view with Download, available regardless of that person's status.
    - **Camera capture for photo upload (added 2026-07-13):** every pass-holder photo field (initial application, add-extra-slot, and the per-person edit form) shows two controls side by side via a shared `PhotoPickerPair` component (`VendorDashboard.jsx`) — the gallery-upload thumbnail, plus a small dedicated camera button (`<input type="file" accept="image/*" capture="environment">`) that opens the device camera directly.
    - **DigitalPassCard visual fixes (2026-07-13, confirmed against a screenshot):** the decorative blurred corner glow behind each card bled past its own card into the surrounding grid and tab header — removed entirely (kept just the card's own tightened `boxShadow`, `0 18px 44px` → `0 8px 20px`). The folded/default face previously never showed the pass holder's name at all (only the market name), so two different people's cards looked identical until tapped — the front cover now leads with `personName` (2-line clamp, shrinks below 18px past 16 characters), with market name demoted to a smaller secondary line. The vendor tab's pass-card grid column width was widened (`minmax(170px,1fr)` → `minmax(190px,1fr)`, gap 18→22).
    - **"Reset my Vendor Pass (testing)" (added 2026-07-13):** a small link on every event card with a pass application that clears that vendor+event's `passApp` (`resetMyPassForTesting()` in `VendorDashboard.jsx`, behind a `window.confirm`), so the whole apply → admin review → digital pass flow can be walked through again from scratch. Testing/demo convenience only — same spirit as the admin "Reset to unpaid" button and the `parkOverride` testing toggle already in the codebase.

22. **Digital Parking Pass — one bi-fold card per event day, built on the existing per-day serial field (added 2026-07-14).** Built from a Claude Design handoff (`Sulap Artisan - Parking Pass.zip` → `Parking Pass.dc.html`, a bi-fold flip card distinct from the Vendor Pass card). Unlike Vendor Pass, this needed **no new data model** — `state.parking` (flat map, `${vendorId}-${eventId}-${dayIndex}` → serial string) already existed and already held exactly the one thing a pass needs that isn't derivable elsewhere: the per-day serial admin assigns (previously labeled "ticket" in the admin UI, renamed to "serial" to match). Everything else the card shows comes straight off existing records — vendor business name + car plate (`vendor.business`/`vendor.plate`), market name (`event.name`), market start/end dates (`event.startDate`/`event.endDate`). The per-day "valid until" cutoff is **not** tied to the event's operating hours at all — see the fixed-11:59PM follow-up below.
    - **New component `components/ParkingPassCard.jsx`:** ported 1:1 from the design file — bottom half (always visible: VALID DATE + UNTIL chips, a live "TIME LEFT TO EXIT" countdown pill that ticks every second down to that day's exit cutoff, a dashed ticket-stub divider, a barcode generated from the serial's char codes, the serial itself, and a small "P" badge + the Suria Sabah mall logo) and a bi-fold top flap (front/cover face when folded: Sulap logo, "FOR THE DATE" + "DAY N"; back face when unfolded, becomes the visual top half: amber banner with a faint giant "SULAP" watermark, "MARKET STARTS"/"MARKET ENDS", then "VEHICLE NUMBER" (gradient-text plate) and "VENDOR" (business name)). Tap anywhere on the card to fold/unfold, matching `DigitalPassCard.jsx`'s existing interaction pattern. Fixed dark amber/cream palette, not theme-aware — same "physical badge" convention as `DigitalPassCard`. **No decorative corner glow** — `DigitalPassCard`'s prior fix (glow bled outside the card into the surrounding grid) was applied preemptively here by just not including one.
    - **The generic raster "P" parking-sign icon from the design file was deliberately not imported as an asset** — recreated as a small styled div (solid amber square, cream "P") instead, reusing the same visual language as the card's own outlined "P" badges elsewhere on its faces, so only one real new image asset was needed: `public/assets/suria-sabah-logo.png` (`sulap-lockup.png` already existed from the Vendor Pass work).
    - **New date/time helpers in `lib/helpers.js`:** `parseDateOnly` (parses a "YYYY-MM-DD" string as local midnight — avoids the UTC/local off-by-one `new Date('YYYY-MM-DD')` risks in negative-UTC timezones), `eventDayDate` (the calendar date for day N of an event), `monthDayLabel` ("26 JULY" formatting), `fmtTime12` ("16:00" → "4:00 PM"), `isoLocal` (builds the local-time countdown target from a date + "HH:MM").
    - **Vendor side (`VendorDashboard.jsx` → Parking tab):** each approved event application always renders one `ParkingPassCard` per event day (no more separate "not yet assigned" placeholder tile) — the card itself now carries three states via a `dayStatus` prop (`'active'|'expired'|'locked'`, computed by comparing that day's calendar date against today, same as the existing `today` var used elsewhere in this file).
    - **Admin side (`AdminDashboard.jsx` → Parking tab):** unchanged structure (per-vendor card, one input per event day, time-locked per rule 6) — only the visible label changed ("Day N ticket" → "Day N serial", plus the lock banner copy) to match the new pass terminology. `state.parking`/`MERGE_PARKING` were not touched.
    - **Countdown gated on check-in, not just the day (same day, follow-up).** Entering a day's serial is treated as the moment that vendor's vehicle actually entered the lot (a human/security process, not automatic) — so the live "TIME LEFT TO EXIT" countdown only appears once a serial exists for that day. Before that, today's card (still fully interactive) shows **"AWAITING PARKING DETAILS"** in the same pill instead of a countdown — with the same pulsing dot the countdown pill uses (kept intentionally, so the pill still reads as "live" while waiting on admin, not dead/inactive). Only `expired`/`locked` cards drop the dot, since those really are inactive.
    - **Only today's pass can be folded open; other days are locked shut and greyed out (same day, follow-up).** `dayStatus` drives both interaction and palette: `'active'` (today) is the only foldable state, in the original warm amber/cream palette. `'expired'` (day already passed) and `'locked'` (day hasn't arrived) both render fully closed with no click handler (`ParkingPassCard`'s `interactive` flag gates the `onClick`, and `effectiveOpen` forces the visual to stay closed even if `open` state was somehow true) and swap to a desaturated grey palette (`MUTED_PALETTE` vs `ACTIVE_PALETTE` in `ParkingPassCard.jsx` — every color the card uses, including the Suria Sabah logo via a `grayscale` filter, comes from whichever palette object is selected, so no color is hardcoded twice). The countdown pill itself becomes a static **"EXPIRED"** or **"LOCKED"** badge (no dot), and the hint line below the card reads "Pass expired" / "Unlocks `<date>`" instead of "Tap to unfold".
    - **Exit cutoff fixed at 11:59PM daily, independent of the event's operating hours (2026-07-14, follow-up).** Initially the "UNTIL"/countdown target was the event's `endTime` (its public closing hour, e.g. 4pm) — changed to a flat `PARKING_EXIT_TIME = '23:59'` constant (`VendorDashboard.jsx`) passed into both `fmtTime12`/`isoLocal` calls instead of `ev.endTime`, so every pass reads "UNTIL 11:59 PM" and counts down to that, regardless of when the market itself closes.
    - **Today's card sorts first (2026-07-14, follow-up).** Within one event's day cards, `active` no longer sits wherever its day number falls chronologically — `orderedDayInfo` (`VendorDashboard.jsx`) stable-sorts the `active` day to the front, leaving the rest (expired/locked) in their original day order behind it. E.g. on day 2 of a 3-day event, the order is Day 2 (active), Day 1, Day 3 — not Day 1, Day 2, Day 3.

23. **Admin Events tab redesign — sectioned form + status-card grid (2026-07-15).** The old Create Event form was one flat stack of ~10 fields, and Existing Events was a plain `.admin-cards` list of compact rows (tiny 76×46 color swatch, name, date range, one line of meta) — asked to be modernized while keeping the existing amber/cream theme and improving mobile behavior. No data model changes; purely presentation.
    - **Create Event form (`AdminDashboard.jsx`) now grouped into three labeled sections** — Basics, Schedule, Pricing — via a new `SectionHead` component (icon chip + uppercase label), separated by thin dividers instead of one continuous field stack. Basics gained a live preview banner (64px gradient strip showing the event name in Marcellus, using whichever theme color is selected) above the name field, and the 6-swatch theme picker moved from tiny 28px squares to 32px swatches with a double-ring `boxShadow` + checkmark icon on the selected one. Schedule and Pricing switched from manual `display:flex,gap:10` field pairs (which never actually stacked on mobile — just squeezed narrower) to the existing-but-previously-form-grid-only `.form-grid`/`.span2` CSS classes (`index.css`, already used by `VendorRegister.jsx`) — 1 column under 768px, 2 columns above, `.span2` for full-width fields like "Last date to apply". The separate duration banner and two floating F&B/Non-F&B total boxes were unified into one bordered "Pricing preview" panel.
    - **Existing Events switched from `.admin-cards` to `.event-cards`** (`index.css`) — a responsive grid class (1/2/3 columns at the same breakpoints as `.admin-cards`, plus the same hover-lift) that was already defined for this exact purpose but had never actually been wired up to any tab until now. Each card gained: a 64px gradient banner (same swatch color, doubling as a mini version of the create-form preview) with a **derived status badge** top-right (`eventStatus()`, a new pure function near `NoSearchMatch` — events carry no stored status field, so it's computed from today vs. `startDate`/`endDate`: `Upcoming` / `Ongoing` / `Concluded` / `Dates TBC`), a location row (`Icon name="pin"`), and a stats footer (applied count, day count, and an `Applications open`/`closed` tag derived from `lastApp` vs. today — independent of the event-dates status, so e.g. Borneo Makers Fair correctly shows `Upcoming` + `Applications closed` at the same time).
    - **Edit Event modal (`EventDetailModal`, `components/Modals.jsx`) mirrors the same three-section layout** via a new `ModalSectionHead` (a fixed-light-color twin of `SectionHead`, since `Sheet` content doesn't follow admin night mode — see rule 8/19) and the same `.form-grid`/`.span2` classes, plus gained the live preview banner and upgraded swatch picker the create form has. A "Location" field (which only ever existed in the edit modal, not the create form — location defaults to `'Suria Sabah Mall'` on creation) stayed put in Basics.
    - **Verified in both light and dark admin themes and at mobile width (375px)** — `.form-grid` fields confirmed stacking to full-width single column via bounding-box checks, `.event-cards` confirmed 1-column on mobile, and dark-mode CSS var resolution confirmed correct on the new section-head chips and status pills.

24. **Admin Events tab — layered create-wizard + horizontal rail redesign (2026-07-15, same-day follow-up to rule 23), design reference only, no data model changes.** User supplied a standalone HTML/CSS mockup (a `.dc.html` design-tool export, warm amber "Events Dashboard" screen) as a pure visual reference — its own sidebar/nav was explicitly out of scope, only the events-tab content layout. Three flow decisions were confirmed with the user before implementing (not assumed): adopt the mockup's 4-step wizard for Create Event (a real flow change, replacing rule 23's single-scroll sectioned form), adopt the horizontal-scroll rail for Existing Events (replacing rule 23's `.event-cards` grid), and keep the app's existing fonts rather than importing the mockup's Marcellus/Karla Google Fonts (already used app-wide via literal `font-family` values, so no new font loading needed).
    - **Create Event is now a 4-step wizard** (`AdminDashboard.jsx`, `aTab==='events'` block) — Picture (theme-color swatch picker + live 4:5 portrait preview, replaces rule 23's flat 64px banner), Details (event name only — no location field, preserving the existing rule that location defaults to `'Suria Sabah Mall'` and is only editable via the edit modal), Schedule (start/end date, daily times, duration pill, last-apply date — unchanged fields, still `.form-grid`/`.span2`), Pricing (F&B/Non-F&B rates + pricing preview, unchanged). Step position is local component state (`eventStep`, `useState`, reset to 0 on every tab switch and after a successful create) — **not** persisted to the global store, since it's presentational only. Step dots are freely clickable (jump to any step directly, no forced linear gating) matching the form's pre-existing lack of inter-section validation; the only validation is still the final "event name required" check on Create. The step-content wrapper is keyed by `eventStep` and reuses the existing `.tab-panel`/`tabIn` fade-up animation (same easing, same `prefers-reduced-motion` fallback already defined in `index.css`) so switching steps replays the same Apple-style motion already used for tab switches elsewhere — no new animation was introduced.
    - **Existing Events is now a horizontally-scrolling rail of portrait (4:5) cards** (`.event-rail`/`.event-rail-card` in `index.css`, replacing rule 23's `.event-cards` grid, which was removed since nothing else referenced it) — scroll-snap, edge-fade `mask-image`, round prev/next arrow buttons (`useRef` + `scrollBy`), each card showing the theme-color/photo banner with a status badge (top-right) and a circular pencil edit-button overlay (top-left, in addition to the whole card still being clickable to edit, matching the mockup's affordance without changing the existing click-to-edit behavior), then name/location/date/time and an Applied/Days/Applications-open-or-closed footer — same data as the old grid cards, just restyled.
    - **Layout is responsive, not a straight port of the (desktop-only) mockup:** stacked vertically (wizard card above the rail) below 1140px, since the admin shell's own sidebar+content frame doesn't have desktop-mockup-width space until then; from 1140px up, the two panels sit side by side with the rail visually tucked behind the wizard card (negative margin + inset shadow), mirroring the mockup's layered look. `SectionHead` (the icon-chip helper added in rule 23) was removed from `AdminDashboard.jsx` as dead code once the wizard stopped using labeled sections — `ModalSectionHead` in `Modals.jsx` is unaffected and still used by the edit modal.
    - **Edit Event modal got a matching visual-only polish** (`EventDetailModal`, `components/Modals.jsx`) — the flat 64px banner became the same 4:5 portrait treatment (gradient scrim + name overlay) used in the wizard's Picture step and the rail cards, for visual consistency. The modal's own flow (single scrolling Basics/Schedule/Pricing form, not a wizard) and its Location field were intentionally left unchanged — only Create Event became a wizard, per the user's explicit choice.
    - **Verified via DOM/layout inspection** (screenshot capture and smooth-scroll animation were both non-functional in this session's browser pane — consistent with a headless-rendering/compositor limitation, confirmed by disabling `scroll-snap-type` and observing `scrollBy` then working exactly as expected): bounding-box checks confirmed the wizard/rail stack at 375px and 768px and go side-by-side with the correct `-40px` overlap at 1280px; all 4:5 banners measured exact 0.80 ratio; full wizard walkthrough (Picture → theme swatch → Details → name → Schedule → dates/duration pill → Pricing → SST total math) and event creation were exercised end-to-end and the new event appeared correctly in the rail with the wizard reset to step 1; dark-mode CSS var resolution confirmed correct on the wizard card and rail panel backgrounds/text.

25. **Admin Events tab fixes — un-layer the wizard/rail overlap, real photo upload replacing theme-color swatches, events search (2026-07-16, follow-up to rule 24).** User compared the live tab against the original reference mockup and flagged three regressions from rule 24's layered redesign: the create-wizard card visually covered part of the existing-events rail, there was no search affordance despite the mockup showing one, and the Edit Event popup only let admin pick a theme-color swatch — it couldn't actually change the event's photo.
    - **Layered overlap removed** (`index.css`) — the `@media (min-width:1140px)` rule for `.event-wizard-wrap`/`.event-wizard-card`/`.event-rail-panel` dropped the `-40px` negative margin, `78px` compensating padding, `z-index` stacking, and inset shadow that made the rail tuck behind the wizard card. Now a plain side-by-side flex row with a `20px` gap — verified via bounding-box check (`event-rail-panel` left edge starts exactly `20px` after `event-wizard-card`'s right edge, no overlap) at 1400px width.
    - **Real photo upload replaces the theme-color swatch picker**, in both the create wizard's Picture step (`AdminDashboard.jsx`) and the Edit Event modal (`EventDetailModal`, `components/Modals.jsx`). Events already stored `img` as a raw CSS `background` value (seed data used `url('/assets/...') center/cover no-repeat`; only the create/edit forms had been swatch-only) — so no data model change was needed, just wiring a real `<input type="file" accept="image/*">` → `FileReader.readAsDataURL` → `eventImgFromFile()` (new helper, `mockData.js`, wraps the data URL as `url("...") center/cover no-repeat`) into both forms. `isEventPhoto(img)` (`mockData.js`) distinguishes an uploaded photo (`url(...)` string) from the gradient fallback, driving the button label ("Upload photo" vs "Change photo") and whether a "Remove" button (resets to `EVENT_IMG_PALETTE[0]`) shows. The now-inaccurate "Real photo upload isn't available yet (needs cloud storage)" disclaimer was removed from both forms. The 6-swatch `EVENT_IMG_PALETTE` still exists as the silent fallback background for events with no uploaded photo — it's no longer a user-facing picker.
    - **Events search added** to the Existing Events rail header (`AdminDashboard.jsx`) — a circular search-icon button (matching the reference mockup) toggles an inline text input (`eventSearch`/`eventSearchOpen` local state, reset on tab switch like `vendorSearch`) that filters the rail by event name; the "N total" count reverts to showing while search is closed, and a "No events match" empty state distinguishes a search miss from a genuinely empty events list.
    - **Verified end-to-end in the browser pane** (screenshot capture non-functional in this session, same as rule 24 — verified via `read_page`/DOM inspection and `javascript_tool` instead): opened the Events tab and confirmed "Upload photo" replaces the swatches in the wizard; opened Edit Event on a seeded event (photo already a `url(...)`) and confirmed it correctly showed "Change photo" + "Remove"; simulated a real file upload via a synthetic `File`/`DataTransfer` dispatched on the file input, confirmed the `data:` URL landed in the preview's inline `background` style, saved, and confirmed the same `data:` URL now appears on the rail card; clicked the search icon, typed "Harvest", confirmed the rail filtered to just Harvest Night Market, and confirmed clearing it restored all 3 events; bounding-box check confirmed no wizard/rail overlap at 1400px. No console errors at any step.

```
pending ──approve──► approved ──suspend──► suspended
   │  ▲                  │                      │
   │  └──reconsider──┐   │                      │
   └──reject──► rejected └────────reinstate─────┘
```
(Auto-approve setting on: registration goes straight to `approved`, bypassing `pending` entirely.)

**Mock data fix (2026-07-11):** the original seed data had Borneo Brews (`v2`) and Pulau Soap Co. (`v4`) marked `status: 'pending'` while already carrying event applications, a payment record, offense history, and (for Pulau Soap Co.) an approved shared booth — which violates rule 2 above. Both were corrected to `approved` since their data already reflects real event participation. Two new vendors (`v6` Kadazan Silver, `v7` Rumah Anyaman) were added as clean `pending` examples with no event footprint, to keep the Vendor Applications queue demo intact.

## Known gaps / not wired yet

- **No real authentication** for either vendor or admin login — both are mocked, always logging in as fixed accounts. **Status gate added 2026-07-12** (see Vendor Login above) makes the mock respect registration status, but there's still no real password check and no real "you've been approved" email — the login screen is the honest stand-in for that email/invite step.
- **"Email alerts" is a mock signal, not real email** — the toast note ("· vendor emailed") is cosmetic confirmation that the setting is respected; no actual email is sent anywhere in this prototype.
- **"Show events publicly" toggle is still not wired** — flipping it doesn't currently hide events from the public home page.
- **Payment advice / invoice / receipt uploads are all mock toggles, not real files** — vendor's "upload" and admin's "upload"/"download" buttons just flip a boolean and show a toast; no file is ever actually stored or transferred anywhere. Needs real storage (Phase 2) for any of it to be end-to-end. Fixed 2026-07-11: vendor-side upload UI now exists (it didn't before), so the *flow* is complete — just not backed by real files yet.

## Phase 2 / 3 — not started

- **Supabase:** no tables wired, everything is mock data in `src/data/mockData.js` + in-memory store (`src/lib/store.jsx`). Needs: vendors, events, applications, payments, deposits, offenses, passes, parking, activity tables + real auth, and a real email/notification provider.
- **Netlify:** not deployed, no env vars configured.
